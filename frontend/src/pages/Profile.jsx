import React from 'react'
// Contains user information like: Name, Email, Password, Address & Phone Number from Order Model. And Options to edit everything except email address which will be verified via otp send through mail.
const Profile = () => {
    return (
        <div>Profile</div>
    )
}

export default Profile